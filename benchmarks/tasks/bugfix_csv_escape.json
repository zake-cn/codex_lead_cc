{
  "benchmark_id": "bugfix_csv_escape",
  "mode": "codex_lead_cc",
  "goal": "Fix CSV field escaping in the demo project and add regression tests.",
  "workers": ["scout", "implementer", "tester", "reviewer"],
  "success_criteria": [
    "implementer generates a patch in an isolated worktree",
    "tester reports passing tests after permission approval",
    "reviewer returns approve or request_changes with findings"
  ]
}
